# N5 · i18n 曹 — 审计

分支 `feature/njc/drop-subscription-pool`。独占文件：`i18n/keys.go`、`i18n/locales/*.yaml`、`web/default/src/i18n/**`。
未越界，未跑 `git add` / `git commit`。

## 一、总账

| 面 | 删除 | 新增 |
|---|---|---|
| `i18n/keys.go` | 11 个 `MsgSubscription*` 常量 + 块注释（整块 15 行） | — |
| `i18n/locales/{en,zh-CN,zh-TW}.yaml` | 各 11 条 `subscription.*` + 块注释（各 13 行） | — |
| `web/default/src/i18n/locales/*.json`（6 语言） | 各 38 个 key（凡该文件中存在的全删） | en/zh 各 +7（N4 新引入的充值档位文案） |
| `web/default/src/i18n/static-keys.ts` | 30 条静态注册 | 1 处块注释改名 |

净计：后端 -54 行；前端 locale 各删 38 key；`static-keys.ts` 375 条（原 405）。

## 二、判据

1. **后端**：`MsgSubscription*` 11 个常量在 HEAD 上**全仓零引用**（`git grep MsgSubscription HEAD` 只命中 `keys.go` 自身），属死常量 → 整块删除，无回归面。
2. **前端**：对每个候选 key 做「是否仍被 `web/default/src/`（排除 `i18n/`）任何 `.ts/.tsx` 引用」的全量子串扫描。
   - **仍被引用的一律不删**。
   - 只删「本地订阅额度池」语义且**已因本次重构失去引用**的 key。
   - 执行时机：等 N4 的前端文件集连续 60s 无变化（`git status --porcelain -- web/default/src` 哈希稳定 250s）后才动手，避免与 N4 并发写产生误判。
   - 删完做了**回race复查**：38 个已删 key 在 `src/` 中零引用（见下方验证）。

## 三、删了什么（前端 38 key，按语义分组）

**用户侧订阅额度池卡片**（`my-subscription-card.tsx` 已被 N4 整删）
`Active subscription` / `No active subscription` / `My Subscription` / `My Subscriptions` /
`Subscription usage` / `Choose a plan to start using subscription quota.` / `No Active`

**订阅 vs 钱包计费偏好（BillingPreference，随额度池消失）**
`Subscription First` / `Subscription Only` / `Wallet First` / `Wallet Only` /
`Preference saved as {{pref}}, but no active subscription. Wallet will be used automatically.`

**订阅购买入口**
`Purchase Subscription` / `Subscription Plans` / `Subscription purchased successfully` /
`Subscribe Now` / `Subscribe to a plan for model access`

**时长 / 重置周期（架构决定「额度永久有效、不限时」→ 死概念）**
`Validity Period` / `Reset Period` / `Quota Reset` / `No Reset` / `Custom (seconds)` /
`Duration Settings` / `Duration Unit` / `Duration Value` / `Custom Seconds` / `Raw Quota`

**管理端订阅套餐 CRUD 文案**（`features/subscriptions/**` 已被 N4 整删）
`Create new subscription plan` / `Modify existing subscription plan configuration` /
`Fill in the following info to create a new subscription plan` / `No subscription plans yet` /
`Click "Create Plan" to create your first subscription plan`

**管理端用户订阅池管理**（`user_subscriptions` 表已删）
`User Subscription Management` / `Manage Subscriptions` / `Add subscription` / `No subscription records` /
`After invalidating, this subscription will be immediately deactivated. Historical records are not affected. Continue?` /
`Deleting will permanently remove this subscription record (including benefit details). Continue?`

后端 11 个：`subscription.not_enabled` / `title_empty` / `price_negative` / `price_max` /
`purchase_limit_negative` / `quota_negative` / `group_not_exists` / `reset_cycle_gt_zero` /
`purchase_max` / `invalid_id` / `invalid_user_id`。

## 四、疑似孤儿但**保守保留**（交 N6 收口判断）

| key | 现状 | 保留理由 |
|---|---|---|
| `Deducted by subscription` | 已无引用（N4 改了 `common-logs-columns.tsx`） | 红线 4：`billing_source` 字段保留，历史日志里仍有 `subscription` 值；若 N4/N6 确认历史日志不再展示该来源，再删 |
| `Subscription Billing` | 已无引用（`details-dialog.tsx`） | 同上，历史日志详情来源标签 |
| `Subscription plan creation and changes are locked until the administrator confirms compliance terms in Payment Gateway settings.` | 已无引用（`features/subscriptions/index.tsx` 被整删） | 合规确认文案；若将来补回「充值档位管理台」大概率复用 |
| `Purchase Limit` / `Purchase limit reached` / `Limit Reached` | 已无引用 | 限购属于 `SubscriptionPlan`（D1 保留，降级为充值档位）的属性，不是额度池概念 |
| `Balance code adds quota; plan code activates a subscription plan; promo code discounts plan purchase` | HEAD 上**就已经是孤儿** | 本次重构之前就死了，不属本轮改动面（红线 1：改动面趋近于零） |
| `Balance codes credit your balance instantly; plan codes activate the plan right away.` | 同上 | 同上 |
| `Balance pays for subscription plans. Add funds first, then complete this purchase.` | 同上 | 同上 |
| `Gateway payment buttons are currently hidden for subscription checkout.` | 同上 | 同上 |
| `Subscription plan checkout uses balance only.` | 同上 | 同上 |
| `Waiting for payment confirmation... Your subscription will be activated automatically once the payment completes.` | 同上 | 同上 |
| `Plan Code` | 同上 | 同上 |

**必须永久保留、已确认未动**（仍被引用）：
`Subscription` / `Subscription Plan` / `Subscription Management` / `Select subscription plan` /
`Please select a subscription plan(.)` / `Manage subscription plans and pricing.` /
`Subscription sales` / `Top-up + subscription, period` —— 对应 D1 保留的 `SubscriptionPlan`/`SubscriptionOrder`；
Waffo Pancake 三条长文案、两条合规确认文案、`distribution/agent-admin.tsx` 四条分销文案（D10 零改动）。
后端 `payment.compliance_required`（含 "subscription" 字样）**未动**。
OpenAI 兼容 billing（`controller/billing.go`）不走 i18n key，无涉及。

## 五、新增 7 个 key（N4 引入的充值档位文案，只有 i18n 曹能落盘）

`Recharge Tiers` / `Recharge Tier` / `Recharge Now` / `Confirm Recharge` / `Credited Quota` /
`Recharge credited successfully` / `No recharge tiers available`

只加进 `en.json` + `zh.json`（这两个 locale 历来 100% 对齐），fr/ja/ru/vi 走既有
`bun run i18n:sync` 待译流程。**若 N4 后续改了这些文案，需同步改 key。**

## 六、条目数一致性（编码契约 1）

删除对 6 个语言文件**完全对称**：38 个 key 在每个文件中「存在即删」，无单删中文/漏删英文。

| 文件 | 改前 | 改后 | 删 | 增 |
|---|---|---|---|---|
| en.json | 5109 | 5078 | 38 | 7 |
| zh.json | 5109 | 5078 | 38 | 7 |
| fr.json | 4945 | 4907 | 38 | 0 |
| ja.json | 4945 | 4907 | 38 | 0 |
| ru.json | 4945 | 4907 | 38 | 0 |
| vi.json | 4945 | 4907 | 38 | 0 |

**注意：fr/ja/ru/vi 相对 en 的缺口是本轮之前就有的**（改前 en−fr = 165），本轮删除未扩大它；
改后缺口 172 = 165 + 7 个新增待译 key。en/zh 依旧完全对齐（互相 0 缺 0 多）。
每个 locale 文件另有 1 个 en 中不存在的 key，同为改前既有状态，未动。

后端 yaml：`en/zh-CN/zh-TW` 改前各 231 条 → 改后各 220 条，三份完全对称。

## 七、验证记录

- `gofmt -l i18n/` → 无输出（干净）
- `yaml.safe_load` 三份 yaml → OK，`subscription.*` 残留 0
- `json.load` 六份 locale → OK；en/zh 键集完全相等
- `static-keys.ts` 用 node eval 解析 → OK，375 条，**全部存在于 en.json**（零悬空注册）
- 反向复查：38 个已删 key 在 `web/default/src/`（排除 `i18n/`）中引用数 **0**
- `rg MsgSubscription` 全仓 → 0 命中

## 八、遗留风险 / 交给 N6

1. `static-keys.ts` 里 `// Subscription management` 与 `// User subscription management` 两个**块注释**已过时
   （对应功能被 N4 删除），但块内剩余条目多为 `Active`/`Price`/`Edit`/`Loading...` 之类通用串，
   贸然删会被 `i18n:sync` 连带裁掉别处仍需要的翻译 → **只留注释未删条目**，请 N6 在
   `features/**` 定型后统一收口。
2. 上表「疑似孤儿但保守保留」11 条，N6 全仓编译 + `bun run i18n:sync` 后可再判一次。
3. 本轮**未跑** `bun run i18n:sync`（会连带重排/裁剪与本任务无关的 400+ 条既有孤儿 key，
   超出改动面红线）。建议 N6 决定是否统一跑一次。
4. 未动 `web/classic/`（不在派工范围，且是独立 i18n 体系）。
