# N4 · 前端曹 — 审计

分支：`feature/njc/drop-subscription-pool`
范围：`web/default/src/features/**` `web/default/src/routes/**` `web/default/src/hooks/**`
（未触碰 `src/i18n/**` —— N5 的）

## files_written

| 文件 | 动作 |
|---|---|
| `web/default/src/features/subscriptions/**`（17 文件） | **整目录删除** |
| `web/default/src/routes/_authenticated/subscriptions/index.tsx` | **删除** |
| `web/default/src/features/wallet/components/my-subscription-card.tsx` | **删除**（240 行） |
| `web/default/src/features/wallet/components/subscription-plans-card.tsx` | **删除**（655 行，死代码，见「偏离」） |
| `web/default/src/features/wallet/components/recharge-tier-card.tsx` | **新增**（120 行，档位卡展示层） |
| `web/default/src/features/wallet/components/purchase-plans-tab.tsx` | 重写为充值档位栅格（284 → 147 行） |
| `web/default/src/features/wallet/components/dialogs/plan-purchase-dialog.tsx` | 重写为「确认充值」弹窗（232 → 190 行） |
| `web/default/src/features/wallet/components/wallet-tabs.tsx` | tab 措辞 `Buy Plans` → `Recharge Tiers`；Crown → Coins |
| `web/default/src/features/wallet/index.tsx` | 删 `MySubscriptionCard` + `subscriptionRefreshKey` + `goPlansTab` |
| `web/default/src/features/wallet/api.ts` | 新增 `getRechargePlans` / `payRechargePlanWithBalance` |
| `web/default/src/features/wallet/types.ts` | 新增 `RechargePlan` / `RechargePlanRecord` / `RechargePlanPayRequest` |
| `web/default/src/features/wallet/lib/format.ts` | 注释措辞 |
| `web/default/src/features/wallet/components/redemption-card.tsx` | 注释措辞 |
| `web/default/src/features/users/components/data-table-row-actions.tsx` | 删「管理订阅」菜单项 + Dialog + state + import |
| `web/default/src/features/usage-logs/components/columns/common-logs-columns.tsx` | 删 Cost 列的订阅计费徽章分支 |
| `web/default/src/features/usage-logs/components/dialogs/details-dialog.tsx` | 删「Subscription Billing」DetailSection + `isSubscription` |
| `web/default/src/features/usage-logs/types.ts` | 删 8 个 `subscription_*` LogOtherData 字段 |
| `web/default/src/hooks/use-sidebar-data.ts` | 删「Subscription Management」侧栏项 + `CreditCard` import |
| `web/default/src/hooks/use-sidebar-config.ts` | 删 `admin.subscription` 默认开关 + `/subscriptions` 路由映射 |
| `web/default/src/features/system-settings/maintenance/config.ts` | 删 `admin.subscription` 默认值（与 hooks 同一 seam） |
| `web/default/src/features/system-settings/maintenance/sidebar-modules-section.tsx` | 删订阅模块开关卡 |
| `web/default/src/routeTree.gen.ts` | **codegen 重跑产物**（-22 行） |

## 落在哪个 seam

- **档位数据源**：复用既有 wallet feature seam（`features/wallet/api.ts` + `types.ts`），
  **不新建** `features/recharge-plans/`。删掉 `features/subscriptions/` 后钱包所需的
  3 个 API + 2 个类型直接搬进 wallet，全站只剩一条路径。
- **接口零删除（D2/C7 遵守）**：`GET /api/subscription/plans`、`POST /api/subscription/balance/pay`
  两条路由原样保留，只换前端函数名与措辞。已在 `api.ts` 注释里写明「legacy route kept as-is」。
- **展示层拆分**：档位卡拆成独立 `recharge-tier-card.tsx`，让 tab 保持在 147 行、
  单函数 ≤40 行（原 284 行文件的 map 回调体 78 行超标）。

## 措辞改造（D1：套餐 → 充值档位）

| 旧 | 新 |
|---|---|
| `Buy Plans` | `Recharge Tiers` |
| `Subscribe Now` | `Recharge Now` |
| `Purchase Subscription`（弹窗标题） | `Confirm Recharge` |
| `Subscription purchased successfully` | `Recharge credited successfully` |
| `Plan Name` | `Recharge Tier` |
| `Total Quota` | `Credited Quota` |
| `No plans available` | `No recharge tiers available` |
| — | 新增 `Never expires`（对应 D7「永久有效」） |

新增 key 共 7 个：`Recharge Tiers` / `Recharge Now` / `Confirm Recharge` /
`Recharge credited successfully` / `Recharge Tier` / `Credited Quota` /
`No recharge tiers available` / `Never expires`。**交 N5 补 locale。**
被删组件遗留的孤儿 key（`Subscription Management`、`Manage Subscriptions`、
`Subscription Billing`、`Deducted by subscription`、`Validity Period`、
`Quota Reset`、`Reset Period`、`Purchase Limit`、`Limit Reached`、
`Purchase limit reached`、`Subscribe Now`、`Buy Plans`、`Plan Name`、
`Manage subscription plans and pricing.` 等）同样交 N5 清理。

## 成功后刷新逻辑（本次核心行为变更）

旧：购买成功 → `fetchUser()` + `subscriptionRefreshKey++` 去重拉「我的订阅」卡。
新：档位购买 = 折扣换额度，**只需刷新钱包余额** → `onPurchaseSuccess={fetchUser}`。
`RedemptionCard.onRedeemed` 同理简化为 `fetchUser`。

## 顺带删掉的死管道（无行为影响）

- `PlanPurchaseDialog` 的 `topupInfo` prop：HEAD 版本声明后从未使用，随重写删除，
  `WalletTabs` → `PurchasePlansTab` 的传递链一并收紧（`WalletTabs` 自身仍需 `topupInfo` 给 `PayAsYouGoTab`）。
- 购买次数上限 UI（`max_purchase_per_user` + `Limit Reached` + `getSelfSubscriptionFull`）：
  计数源来自 `user_subscriptions`，D1 已删表 → C7 下 `/subscription/self` 恒返回空列表，
  count 永远为 0、上限永不触发。**留着就是永远为假的死分支**，故整块删除。
- 有效期 / 额度重置显示（`formatDuration` / `formatResetPeriod`）：额度池概念，
  D7 明确档位「永久有效」，改为一行 `Never expires`。

## 跑过哪些验证

| 命令 | 结果 |
|---|---|
| `bun run dev --port 3111`（触发 tanstack router codegen） | ✅ `routeTree.gen.ts` 重生成，`subscriptions` 残留归零 |
| `bun run typecheck`（`tsc -b`） | ✅ 我的文件零错误 |
| `bunx eslint <本轮全部文件>` | ✅ 零 error |
| `bunx prettier --check <本轮全部文件>` | ✅ 全部合规 |
| `bun run copyright:check` | ✅ 新增文件已带 AGPL 头，未新增违规 |
| `rg -n "features/subscriptions" src/` | ✅ 零命中 |
| `rg -i -n "subscription" src/features/wallet src/features/users src/features/usage-logs src/hooks src/routes src/routeTree.gen.ts` | ✅ 只剩 3 处**故意保留**的后端路由字面量与解释性注释 |

**说明**：项目无独立 codegen script（`package.json` 里没有 `routes:generate`）。
tanstack router 走 `@tanstack/router-plugin/rspack`，只在 `rsbuild dev/build` 时生成；
且 `@tanstack/router-generator` 未单独安装到 `node_modules`，无法脱离 rsbuild 跑。
故用 `bun run dev --port 3111` 触发后立刻停掉（3000 端口被占，不能用默认端口）。

## 未提交 / 未解决风险

**未跑 `git add` / `git commit`**（按派工要求）。

### 1. 偏离派工：`subscription-plans-card.tsx` 是删不是改造

派工写「改造成充值档位卡」，但 `rg` 证实该 655 行文件**全站零引用**（无 barrel、无 import），
是死代码；活路径是 `purchase-plans-tab.tsx` → `plan-purchase-dialog.tsx`。
改造死代码等于凭空造第二条档位展示路径（违反「不造平行路径」），且 655 行远超 300 行粒度红线。
**决定：删除，改造只做在活路径上。** 如门下省认为必须保留，可从 git 恢复。

### 2. ⚠️ 管理端「充值档位 CRUD」UI 随目录整删而消失（需上层裁决）

C7 明确「admin 套餐 CRUD **保留**（现在管的是充值档位）」，后端 4 条 admin plan 路由不删；
但派工要求整删 `features/subscriptions/`，其中就包含唯一的档位管理界面
（`subscriptions-table` / `subscriptions-mutate-drawer` / `plan-form`）。
**结果：后端能管、前端没入口，管理员只能调 API 建档位。**
按派工执行了，但这是 D1 与派工粒度之间的真实张力，请上层确认是「本轮先这样，
下轮另起 recharge-tiers 管理页」还是「需要我把 plan CRUD 页面搬进 wallet/system-settings」。

### 3. 兑换码 `type === 'plan'` 分支未动（跨 seam，等后端定）

`wallet/components/redemption-card.tsx:108-121` 仍有
`t('Plan "{{title}}" activated')` + Crown 图标分支，依赖后端 `RedeemResultData.type`。
D1 下「档位兑换码」应该改成加余额，但返回值是 `type: 'balance'` 还是仍为 `'plan'`
由 N1/N2 决定，我无法从前端单方面确认，**故保持原样**。
后端定稿后需要有人回来对齐这块措辞（大概率能直接删 plan 分支）。

### 4. 遗留他人报错（未修，非我范围）

`bun run typecheck` 另有 4 个**先于本轮存在**、且文件本轮未被任何人修改的错误：

- `src/components/ai-elements/code-block.tsx:30` — `Cannot find module 'hast'`
- `src/components/layout/components/footer.tsx:45,127` — 未使用变量 ×2
- `src/features/redemption-codes/index.tsx:30` — `fixedContent` prop 不存在
- `src/features/system-settings/billing/index.tsx:105` — `XunhuEnabled` 不在 `BillingSettings`

同理 eslint 有 18 条 `react-hooks/set-state-in-effect` 存量告警，均在我未改动的文件里。
**按派工「只修自己文件的类型错」原则，全部未动。**

## 远端 / 并发情况

- 未 `git fetch`（派工限定本地改文件，无提交动作）。
- 观察到 **N5 正在并发修改** `web/default/src/i18n/locales/*.json` 与 `src/i18n/static-keys.ts`，
  以及 `i18n/` 下的 Go 侧 yaml —— 与我零重叠，未触碰。
- 本轮开始时 `git status` 已有 N1/N2 的 `service/billing_session.go`、`service/funding_source.go` 等改动，
  未回滚、未裹进任何操作。
